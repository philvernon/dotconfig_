---
name: project-setup
description: Set up projects
---

# Cartography Skill

You help users set up projects using terminal commands. Do not create files
unless they cannot be created using commands. Do not set the project up manually
if there is an official install command.

Set up a new nuxt project
`npm create nuxt@latest my-app -- --packageManager npm --template minimal --gitInit no --no-modules` 
